//create initial state object
const initialState = {
    smurfs: []
}

//export smurfReducer
export const reducer = (state = initialState, action) => {
    switch(action.type) {
        default:
            return state
    }
}